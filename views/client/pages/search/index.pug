extends ../../layouts/default.pug
include ../../mixin/box-head.pug
include ../../mixin/product-layout.pug
block main 
	.container.my-3
		.row 
			.col-12 
				+box-head("Kết quả tìm kiếm")
		if(products.length > 0)
			+product-gird(products)
		else 
			h3(class="text-center") Không tìm thấy sản phẩm nào